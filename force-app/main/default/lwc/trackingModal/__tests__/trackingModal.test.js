/**
 * trackingModal étend LightningModal et est ouvert de manière programmatique
 * via TrackingModal.open(). Il n'y a pas de rendu DOM direct à tester ici.
 */
describe('c-tracking-modal', () => {
    it('placeholder - composant LightningModal sans rendu direct', () => {
        expect(true).toBe(true);
    });
});